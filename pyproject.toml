[project]
name = "citizen_pm_analytics"
version = "0.1.0"
description = "Analysing particulate matter data from sensor.community (former luftdaten.info)"

[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
